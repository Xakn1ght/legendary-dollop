# Python
__pycache__/
*.py[cod]
*.pyd
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
venv/
.venv/
env/
.envrc

# Secrets / local config
.env
**/.env
postgresql_connection.txt
docs/notes/postgresql_connection.txt

# Logs / artifacts
logs/
*.log
artifacts/

# Backups created by scripts
backups/
*.backup

# Databases (keep these out of git; back them up via tar/backup scripts)
*.db
*.sqlite
*.sqlite3

# App runtime state / local data — blanket: sessions, uploads, receipts,
# user photos. NOTHING under data/ may ever land in git.
src/app/data/
src/app/webapp/admin/uploads/

# Captured network traces / local misc
misc/other/*.har
misc/other/network-tab.txt

# Editor/OS
.vscode/
.DS_Store

# Large archives (keep these out of git history)
*.zip

# Local editor tooling
.impeccable/
skills-lock.json

# Local binaries
bin/


# Database dumps (real customer data — never commit)
*.sql

# Internal notes / planning docs / scratch (kept on the server, not in the repo)
BGs/
docs/astro/
handoff.md
HANDOFF.md
FLOWS_HANDOFF.md
REWARDS_HANDOFF.md
PASHA_CONTEXT.md
PUBLISH_CHECKLIST.md
REFACTOR_PLAN.md
DESIGN.md
PRODUCT.md
asstroo_*.md
pics/
previews/
rewards.html
docs/premium-tier-redesign*/
